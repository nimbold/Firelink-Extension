{
  "name": "firelink-companion",
  "private": true,
  "version": "1.0.13",
  "scripts": {
    "check": "node --check protocol.js && node --check background.js && node --check popup/popup.js && node --test tests/*.test.cjs",
    "test": "node --test tests/*.test.cjs"
  }
}
