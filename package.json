{
  "name": "firelink-companion",
  "private": true,
  "version": "2.0.5",
  "scripts": {
    "build": "node scripts/package-extension.js",
    "check": "node --check protocol.js && node --check background.js && node --check content.js && node --check popup/popup.js && node --check chromium-service-worker.js && node --check scripts/package-extension.js && node --test tests/*.test.cjs && npm run build",
    "test": "node --test tests/*.test.cjs"
  }
}
